# Hermes next delicate decision packet

Status: decision required; no action authorized by this packet

## Recommended next decision

Authorize only a provenance-bound, offline model-artifact and synthetic-evaluation
phase for the Ernie candidate. Keep model selection, credentials, private prompts,
live services, pointers, automations, Bert, deployment, and promotion excluded.

The phase should compare the official Qwen3.8-27B candidate with the current
installed baselines after router/template corrections. It may use public and
synthetic orchestration, tool, coding, reasoning, long-context, privacy-canary,
handoff, latency, memory-pressure, and concurrency cases. It must not use Ernie's
private clone or expose any local content to a model or cloud endpoint.

Required approval inputs before that phase:

- exact official artifact repository, revision, filenames, hashes, license, and
  provenance chain;
- exact local runtime and quantization, with projected peak memory, context, and
  concurrency limits for the 64 GB M1 Max;
- download destination outside Git and shared memory, network/download commands,
  size bounds, failure retention, and rollback/removal handle;
- frozen evaluation cases, baseline identities, promotion thresholds, and an
  explicit statement that evaluation success does not change model selection.

Stop for provenance ambiguity, third-party derivative substitution, unexpected
network/install behavior, forbidden dependency evidence, memory-pressure risk,
or a request for private prompts.

## Later boundaries

| Boundary | Safe without private/live authority | Requires separate delicate authority |
| --- | --- | --- |
| Credential-bound closed runtime | Synthetic configuration validation and redacted command planning | Reading credentials, starting the closed credential-bound runtime, any private prompt, or any external model endpoint |
| Final Ernie delta and promotion | Static delta plan, paired-pointer preconditions, health/rollback command review | Live profile delta, credential/config migration, writer quiescence, service stop/start, pointer switch, production traffic, observation, or rollback execution |
| Task 13 activation | Synthetic monitor/inbox tests and disabled code | Creating, changing, pausing, resuming, or activating any schedule/automation; the legacy health automation must not overlap its replacement |
| Bert phase | Sanitized fixtures and host-independent plan | SSH/live access, status/heartbeat capture, backup, deployment, service change, pointer switch, Telegram/gateway validation, or promotion |

## Promotion gates retained

- Final Ernie promotion requires a fresh immutable backup, verified rollback
  pair, final delta integrity, credential-bound closed health, model decision,
  schedule reconciliation, atomic paired-pointer rehearsal, and an observation
  window with automatic pre-traffic rollback.
- Bert remains a separate later approval after Ernie evidence is accepted. The
  active legacy Bert health automation is a final-promotion blocker until it is
  explicitly paused; the Computer History heartbeat path adaptation remains a
  separate approval-gated change.
- No CLEAR receipt grants authority. Generic, stale, or digest-mismatched approval
  evidence is ineligible.
